plugins {
    id 'java'
}

group 'com.kikomi.jsonpath'
version '1.0'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile(group: 'org.json', name: 'json', version: '20180130')
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
